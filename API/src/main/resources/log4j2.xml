<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<properties>
		<property name="name">service</property>
		<property name="pattern">%d{yyyy-MM-dd HH:mm:ss.SSS} | %-5.5p | %C:%-5.5L | %msg%n</property>
		<!-- 
		It will look like that:
		2013-04-03 07:37:51.993 | WARN  | main       | Class.Server:56    | My app is logging stuff
		-->
	</properties>
	
	<appenders>
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout pattern="${pattern}"/>
		</Console>
		<RollingFile name="RollingFile" fileName="logs/${name}.log" filePattern="logs/$${date:yyyy-MM}/${name}-%d{yyyy-MM-dd}-%i.log.gz">
			<PatternLayout>
				<pattern>${pattern}</pattern>
			</PatternLayout>
			<Policies>
				<!-- Daily file rotation unless file exceeds 100MB (in which case file is split) -->
				<TimeBasedTriggeringPolicy />
				<SizeBasedTriggeringPolicy size="100 MB"/>
			</Policies>
		</RollingFile>
	</appenders>
	
	<loggers>     
	    <root level="trace">
	        <appender-ref ref="Console" level="info"/>
	        <appender-ref ref="RollingFile" level="info"/>
	    </root>    
	</loggers>
</configuration>